Implement Storage.GetSchemaForPack/SetSchemaForPack (were NOOP)

Closes the schema-hooks runtime gap: the 0x60/0x61 hooks were compiled + arg-checked
but NOOP at runtime. Per-pack typed-field schema is now stored + returned as span
bytes:
- SetSchemaForPack(pack, span): store the schema span bytes for that pack.
- GetSchemaForPack(pack): return them as a fresh arena span (empty if unset).
HostApi (picoscript_vm.py) + the vm/picovm.js mirror, byte-identical: set->get
round-trips exactly and identically Python<->JS. tests/test_devices.py. The typed
field-record format + the schema designer UI build on this. Regenerated playground.html.

Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>
