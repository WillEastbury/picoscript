Server-entry contract: Server.Main { } (C) + SERVER..ENDSERVER (BASIC)

PIOS #6: a clear server-endpoint entry form. Transparent wrapper -- the body is the
program entry, compiling to the endpoint-shaped bytecode the port-82 worker expects
(request-context setup + Net.Status + body). Honours "BASIC not C": the BASIC form
is a block keyword pair, no braces.
- C frontend: Server.Main { ... } (picoscript_cfront.py).
- BASIC frontend: SERVER ... ENDSERVER (picoscript_basic.py).
- Mirrored byte-for-byte in vm/picoc.js (CParser/CLowerer + BParser/BLowerer).
- Transparent: wrapped bytecode == bare body; BASIC SERVER == C Server.Main for an
  equivalent program; Python frontend == JS frontend. tests/test_dsl.py.
Regenerated docs/playground.html. Full suite green.

Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>
